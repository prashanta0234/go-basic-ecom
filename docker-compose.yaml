version: "3.8"

services:
  # app:
  #   build: .
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - MONGODB_URI=mongodb://mongo:27017
  #     - MONGODB_NAME=ecommerce_db
  #     - JWT_SECRET=your_super_secret_jwt_key_here
  #   depends_on:
  #     - mongo
  #   networks:
  #     - app-network

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - app-network

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    networks:
      - app-network

volumes:
  mongo_data:

networks:
  app-network:
    driver: bridge
